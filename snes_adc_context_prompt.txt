You are helping me implement a Super Nintendo (65c816) CPU emulator in Rust.

Context:
- This is a SNES emulator.
- CPU is 65c816.
- Emulation mode vs native mode matters.
- I am using cycle-accurate bus reads with dummy reads.
- I am validating behavior against JSON-based CPU test vectors (initial CPU state, final CPU state, per-cycle bus accesses).

Important constraints:
- Do NOT claim to have read files unless you actually read them.
- Prefer correctness over theory when tests disagree.
- Keep explanations concise unless I ask for detail.
- When implementing methods, do not add excessive comments unless requested.

Previously reviewed helper files (please read them fully before answering anything):
- Addressing helpers:
  https://raw.githubusercontent.com/caklimas/rust-snes/refs/heads/opcode-testing/src/cpu/opcodes/helpers/addressing.rs

- Memory helpers:
  https://raw.githubusercontent.com/caklimas/rust-snes/refs/heads/opcode-testing/src/cpu/opcodes/helpers/memory.rs

Current state of work:
- Opcode 0x5F (EOR absolute long,X) has been implemented correctly.
- Opcode 0x61 (ADC (dp,X)) is implemented.
- Addressing and bank selection issues are fixed.
- Remaining inconsistencies are around ADC decimal-mode overflow (V flag).
- Test vectors disagree on how V behaves when D=1.
- Practical resolution: treat V as unreliable / don't-care in decimal mode for ADC/SBC, or mask it in the test harness.

Key helper involved:
- perform_addition_with_carry_u8 (ADC core logic for 8-bit A)
- set_v_flag_u8 uses standard signed overflow logic.

Last agreed implementation of perform_addition_with_carry_u8 (binary V, nibble-wise BCD):

```rust
fn perform_addition_with_carry_u8(cpu: &mut Cpu, value: u16) {
    let old_accumulator_full = cpu.registers.a;
    let a = old_accumulator_full & 0x00FF;

    let carry_in = get_carry_in(cpu) & 0x0001;
    let v = value & 0x00FF;

    let binary_sum = a + v + carry_in;
    let binary_result8 = (binary_sum & 0x00FF) as u8;

    let decimal = cpu.registers.p.contains(ProcessorStatus::DECIMAL);

    let (final_result8, carry_out) = if decimal {
        let mut lo = (a & 0x000F) + (v & 0x000F) + carry_in;
        let mut hi = (a & 0x00F0) + (v & 0x00F0);

        if lo > 0x0009 {
            lo += 0x0006;
        }
        if lo > 0x000F {
            hi += 0x0010;
        }
        if hi > 0x0090 {
            hi += 0x0060;
        }

        (
            ((hi & 0x00F0) | (lo & 0x000F)) as u8,
            hi > 0x00FF,
        )
    } else {
        (binary_result8, binary_sum > 0x00FF)
    };

    cpu.registers.a = (old_accumulator_full & 0xFF00) | (final_result8 as u16);

    set_nz_flags_u8(cpu, final_result8);
    cpu.registers.p.set(ProcessorStatus::CARRY, carry_out);
    set_v_flag_u8(cpu, a, binary_result8 as u16, v);
}
```

Next steps for the new session:
- Decide whether to formally mask V in decimal mode inside ADC/SBC or in the test harness.
- Continue implementing remaining ADC/SBC addressing modes using the same helpers and conventions.
